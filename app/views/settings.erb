<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Change Password</title>
    <link rel="stylesheet" href="login.css">
<style>
    .btn-go-back-container {
        text-align: center;
        margin-top: 20px;
    }

    .btn-go-back {
        font-size: 60px;
        color: #d9534f; /* Red color */
        text-decoration: none;
        display: inline-block;
        transition: color 0.3s ease;
        border-radius: 50%;
        width: 80px;
        height: 80px;
        line-height: 80px;
    }

    .btn-go-back:hover {
        color: #a94442; /* Darker red color on hover */
    }

    .success-message {
        color: #28a745; /* Green color */
        text-align: center;
        margin-top: 20px;
    }
</style>
</head>
<body>
    <form class="form-signin" action="/change_password" method="post">
        <h1 class="h3 mb-3 font-weight-normal text-center">Change Password</h1>
        <label for="current-password" class="sr-only">Current Password</label>
        <input type="password" id="current-password" name="current_password" class="form-control" placeholder="Current Password" required autofocus>
        <label for="new-password" class="sr-only">New Password</label>
        <input type="password" id="new-password" name="new_password" class="form-control" placeholder="New Password" required>
        <label for="confirm-new-password" class="sr-only">Confirm New Password</label>
        <input type="password" id="confirm-new-password" name="confirm_new_password" class="form-control" placeholder="Confirm New Password" required>
        <% if @error_message %>
            <p class="mt-2 text-danger text-center"><%= @error_message %></p>
        <% end %>
        <% if @success_message %>
            <p class="success-message"><%= @success_message %></p>
        <% end %>
        <button class="btn btn-lg btn-primary btn-block mx-auto d-block" type="submit">Change Password</button>
        <div class="btn-go-back-container">
            <a href="/dashboard" class="btn-go-back">&times;</a>
        </div>
    </form>
</body>
</html>
